const items=window.GARDEN_ITEMS||[];const grid=document.getElementById('giftGrid');const empty=document.getElementById('emptyState');const search=document.getElementById('searchInput');const filters=[...document.querySelectorAll('.filter')];let active='all';
function card(item){return `<article class="card" data-category="${item.category}"><div class="visual"><span class="tag">${item.collection}</span>${item.icon}</div><div class="content"><h2>${item.name}</h2><p class="description">${item.description}</p><p class="story">${item.story}</p><div class="price">${item.price}</div><div class="meta"><span class="pill">Prioridade alta</span><span class="pill">Desejado</span></div><a class="buy" href="${item.url}" target="_blank" rel="noopener noreferrer">Ver presente</a><div class="note">Valores, tamanhos e disponibilidade podem mudar na loja.</div></div></article>`}
function render(){const term=search.value.trim().toLocaleLowerCase('pt-BR');const filtered=items.filter(i=>(active==='all'||i.category===active)&&`${i.name} ${i.collection} ${i.description}`.toLocaleLowerCase('pt-BR').includes(term));grid.innerHTML=filtered.map(card).join('');empty.style.display=filtered.length?'none':'block';document.getElementById('visibleCount').textContent=filtered.length}
filters.forEach(btn=>btn.addEventListener('click',()=>{filters.forEach(b=>b.classList.remove('active'));btn.classList.add('active');active=btn.dataset.filter;render()}));search.addEventListener('input',render);document.getElementById('totalCount').textContent=items.length;document.getElementById('shareBtn').addEventListener('click',async()=>{const data={title:document.title,text:'Conheça o Jardim de Desejos de Michèlé Joohann',url:location.href};try{if(navigator.share)await navigator.share(data);else{await navigator.clipboard.writeText(location.href);alert('Link copiado!')}}catch(e){}});render();